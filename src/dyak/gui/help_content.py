"""
Офлайн-справка для GUI (T008): разделы для нетехнического кадровика.

Тексты статические, без сети (требование CONCEPT — всё работает офлайн).
Рендерятся в `QTextBrowser` как простой HTML. Источник правды по поведению —
ядро (`cli.py`, README); здесь — дружелюбный пересказ для пользователя.
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class HelpSection:
    """Раздел справки: заголовок (для оглавления) и HTML-тело."""

    title: str
    html: str


HELP_SECTIONS: tuple[HelpSection, ...] = (
    HelpSection(
        'С чего начать',
        """
        <h2>С чего начать</h2>
        <p><b>Дьяк</b> делает пачку одинаковых кадровых документов: на каждую
        строку вашей таблицы — один документ Word, со склонением ФИО,
        должностей и званий по падежам.</p>
        <ol>
          <li>Подготовьте <b>таблицу</b> (.xlsx) и <b>шаблон</b> (.docx).</li>
          <li>На вкладке <b>«Генерация»</b> выберите их, укажите папку для
          результата и нажмите <b>«Сгенерировать»</b>.</li>
        </ol>
        """,
    ),
    HelpSection(
        'Таблица (.xlsx)',
        """
        <h2>Таблица данных</h2>
        <p>Обычная таблица Excel: первая строка — заголовки колонок, дальше —
        по строке на человека/документ.</p>
        <ul>
          <li>Стандартные кадровые колонки (Фамилия, Имя, Отчество, ФИО,
          Должность, Звание и т.п.) распознаются <b>по названию</b> — настройка
          не нужна.</li>
          <li>Колонка «Пол» не нужна — пол определяется автоматически.</li>
          <li>Нестандартные заголовки («Сотрудник», «Наименование должности»)
          распознаются автоматически по содержимому ячеек.</li>
          <li>Скрытые строки и строки, отсечённые автофильтром, в генерацию
          не попадают.</li>
        </ul>
        """,
    ),
    HelpSection(
        'Шаблон (.docx) и падежи',
        """
        <h2>Шаблон документа</h2>
        <p>Шаблон — обычный документ Word, где вместо значений стоят теги в
        двойных фигурных скобках, ссылающиеся на заголовки колонок:</p>
        <p><code>{{ Фамилия }}</code>, <code>{{ Должность }}</code>,
        <code>{{ Дата_начала }}</code></p>
        <p>Пробелы и спецсимволы в заголовке колонки заменяются на
        подчёркивание: «Номер приказа» → <code>{{ Номер_приказа }}</code>,
        «л/н» → <code>{{ л_н }}</code>. Если написать тег с самим спецсимволом
        (<code>{{ л/н }}</code>) — Дьяк поправит его сам и предупредит.</p>
        <h3>Склонение по падежам</h3>
        <p>Падеж задаётся <b>фильтром</b> через вертикальную черту:</p>
        <ul>
          <li><code>{{ Фамилия|ип }}</code> — именительный (кто?)</li>
          <li><code>{{ Фамилия|рд }}</code> — родительный (кого?)</li>
          <li><code>{{ Фамилия|дт }}</code> — дательный (кому?)</li>
          <li><code>{{ Фамилия|вн }}</code> — винительный (кого?)</li>
          <li><code>{{ Фамилия|тв }}</code> — творительный (кем?)</li>
          <li><code>{{ Фамилия|пр }}</code> — предложный (о ком?)</li>
        </ul>
        <p>Именительный можно задать явно — <code>{{ Фамилия|ип }}</code> — или
        просто не ставить фильтр (<code>{{ Фамилия }}</code>): результат тот же,
        как значение в таблице. Несклоняемые колонки (даты, номера) пишутся без
        фильтра.</p>
        """,
    ),
    HelpSection(
        'Генерация',
        """
        <h2>Генерация документов</h2>
        <p>Вкладка <b>«Генерация»</b>:</p>
        <ul>
          <li><b>Таблица</b> — файл .xlsx с данными.</li>
          <li><b>Шаблон</b> — файл .docx с тегами и падежными фильтрами.</li>
          <li><b>Папка результата</b> — куда сложить готовые документы.</li>
        </ul>
        <p>Имена файлов формируются автоматически по колонкам ФИО, лист
        берётся активный. На больших объёмах виден прогресс. Кнопка
        <b>«Отмена»</b> останавливает генерацию; уже созданные файлы
        остаются.</p>
        """,
    ),
    HelpSection(
        'Если что-то пошло не так',
        """
        <h2>Если что-то пошло не так</h2>
        <ul>
          <li>Ошибка показывается понятным текстом в нижней панели — без
          «непонятных трассировок».</li>
          <li>Полный вывод программы виден там же — его можно прокрутить.</li>
          <li>«Не распознаны колонки ФИО» — проверьте, что в таблице есть
          колонки с фамилией/именем/отчеством (или одна колонка ФИО).</li>
        </ul>
        """,
    ),
)
