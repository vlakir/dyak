# Backlog

Парковка идей, побочных находок и «надо бы потом починить».

**Правило:** если в процессе работы над текущей задачей Claude или
Разработчик замечают что-то постороннее — оно идёт сюда, а не в текущий
коммит. Это защищает от расползания scope.

Это **не формальный таск-трекер** со сроками и метриками — это парковка
идей. Но **порядок имеет значение**: сверху — то, что планируется
ближайшим, ниже — менее срочное (FIFO по умолчанию, можно поднимать
приоритетное наверх). Когда из бэклога что-то берётся в работу — оно
вырастает в задачу или спеку (`specs/T<NNN>-…`) и удаляется отсюда.

## Формат

`- **T<NNN>** — [<дата находки>] <короткое описание> — <опционально: контекст / откуда всплыло>`

ID присваивается при создании; новый = `max(существующих T-ID в
BACKLOG.md, BOARD.md и CHANGELOG.md) + 1`. ID не переиспользуется
и сохраняется при перетекании задачи между BACKLOG и BOARD; после
релиза задача переходит в `CHANGELOG.md` (с тем же T-ID), что
гарантирует уникальность между релизами.

## Items

Очередь по этапам реализации из `CONCEPT.md` §17. Берутся по порядку
после закрытия предыдущего этапа.

- **T002** — [2026-06-21] Этап 1 «ФИО»: склонение Ф/И/О через `petrovich`,
  6 русских падежных фильтров (`им/рд/дт/вн/тв/пр`), инициалы в 3 формах,
  **автоопределение пола** (без колонки «Пол», см. ADR 2026-06-21 и T006).
  (`CONCEPT.md` §7.1, §8.1–8.2)
  Acceptance: все 6 падежей корректно склоняют ФИО (м/ж);
  `.инициалы` / `.инициалы_впереди` / `.инициалы_слитно` работают, в т.ч.
  в косвенных падежах; регрессионные фикстуры склонения (`declension.csv`)
  зелёные; пол определяется автоматически (отчество → fallback pymorphy по
  имени → `override` + сигнал в `check` для неоднозначных);
  **сохранение регистра при склонении** — если фамилия записана БОЛЬШИМИ
  буквами (`ИВАНОВ`), её склонения тоже выводятся БОЛЬШИМИ (`ИВАНОВА`,
  `ИВАНОВУ`…); проверяется фикстурой с upper-case фамилией (запрос
  Разработчика 2026-06-21). Зависит от T006 (форма контекста по
  заголовкам, включая разбор одноколоночного ФИО).

- **T003** — [2026-06-21] Этап 2 «Должности»: пословное склонение
  через `pymorphy3`, словарь `overrides` (приоритет над движком),
  `lru_cache`. (`CONCEPT.md` §7.2–7.4)
  Acceptance: должность склоняется по 6 падежам пословно; `MorphAnalyzer`
  инициализируется один раз; override проверяется ДО движка и перекрывает
  его; кеш по `(text, case, gender)`; несклонившийся токен остаётся как
  есть.

- **T004** — [2026-06-21] Этап 3 «Надёжность»: команда `check` (dry-run)
  + отчёт, фильтр `согл(м,ж)`, `StrictUndefined`, сверка пола.
  (`CONCEPT.md` §8.3, §8.6, §10, §13)
  Acceptance: `check` валидирует колонки (вкл. пол), рендерит в память
  без записи, выдаёт отчёт о непросклонившихся словах и о расхождении
  «пол-колонка ≠ пол-по-отчеству»; `согл` даёт правильную форму по полу;
  неизвестная переменная в шаблоне → явная ошибка с именем.

- **T005** — [2026-06-21] Этап 4 «Опции»: `dyak init` (scaffold),
  опц. PDF-экспорт через LibreOffice headless, оптимизация.
  (`CONCEPT.md` §10, §12, §15)
  Acceptance: `dyak init` создаёт `dyak.yaml` + пример шаблона со
  шпаргалкой падежей + пример таблицы; `--pdf` конвертирует вывод через
  `libreoffice --headless` (Linux); крупный прогон укладывается в
  «секунды на сотни строк».
