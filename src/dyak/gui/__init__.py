"""
GUI dyak (T008) — тонкий фронтенд на PySide6 над CLI.

Графическая оболочка для нетехнического кадровика: выбор шаблона/таблицы/
папки и опций, запуск генерации с живым прогрессом, сухой прогон, стартовый
набор и обратная сборка шаблона — всё через вызов ядра `dyak` по
`subprocess` (логика не дублируется). Запуск: ``python -m dyak.gui``.

Доменной логики здесь нет — только сборка команд, разбор их вывода
(`runner`) и виджеты (`app`, `main_window`). PySide6 импортируется только в
этом подпакете, поэтому CLI-сценарий и тесты ядра Qt не тянут. ADR T008 —
в `DECISIONS.md`.
"""

from __future__ import annotations
